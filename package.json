{
  "name": "textforge-ai",
  "private": true,
  "version": "0.1.0",
  "description": "TextForge AI - 知识图谱驱动的 Vibe Coding CLI",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "~5.8.2"
  },
  "engines": {
    "node": ">=18"
  }
}
